<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("./partials/head", {title: "Iniciar Sesión"}) %>
    </head>
    <body class="bg-muted">
        <header class="bg-warning"><%- include("./partials/header") %></header>
        <main>
            <h1>Iniciar Sesión</h1>

            <form action="/auth/login" method="POST" class="login-form">
                <label for="usuario" class="display-none">Usuario</label>
                <% if(typeof errorsUsername != "undefined") {
                errorsUsername.forEach(error => { %>
                <p><%= error.msg %></p>
                <% });%> <% } %> <input type="text" id="usuario" name="username"
                placeholder="Usuario" class="display-block <%= (typeof
                errorsUsername != "undefined") ? "input-error" : "" %>" />
                <label for="password" class="display-none">Contraseña</label>
                <% if(typeof errorsPassword != "undefined") {
                errorsPassword.forEach(error => { %>
                <p><%= error.msg %></p>
                <% });%> <% } %> <input type="password" id="password"
                name="password" placeholder="Contraseña" class="display-block
                <%= (typeof errorsPassword != "undefined") ? "input-error" : ""
                %>" />
                <label for="">Recordame</label>
                <input type="checkbox" name="recordame" id="" />
                <button type="submit">Enviar</button>
            </form>
        </main>
        <footer class="footer-md"><%- include("./partials/footer") %></footer>
    </body>
</html>
